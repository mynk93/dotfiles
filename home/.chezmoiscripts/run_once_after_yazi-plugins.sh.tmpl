#!/bin/sh
# Install yazi plugins declared in ~/.config/yazi/package.toml.
# Runs once per machine after chezmoi has deployed the package.toml file.
set -eu

if command -v ya >/dev/null 2>&1 && [ -f "$HOME/.config/yazi/package.toml" ]; then
    ya pkg install
fi
